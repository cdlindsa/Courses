public final class Player extends Character{

    public Player(int initiativeModifier, int hitPoints, int attackModifier, String name) {
        super(initiativeModifier, hitPoints, attackModifier, name);
    }
}
